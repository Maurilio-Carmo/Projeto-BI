#  FiscalSync — Frontend (.env.example)

# URL base do backend NestJS
# Deixe vazio para usar o proxy do Vite (recomendado em dev local)
# Preencha com o IP/host real para acesso por rede local ou produção
VITE_API_URL=http://localhost:3000
